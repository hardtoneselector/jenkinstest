pipeline {
  agent any
  stages {
    stage('init') {
      steps {
        echo 'init'
      }
    }
    stage('done') {
      steps {
        echo 'bla'
      }
    }
  }
  environment {
    PATH = '/usr/bin'
  }
}